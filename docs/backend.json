
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user's profile information, acting as the central identity for customers, employees, and administrators.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity. (Primary Key)"
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The full name of the user."
        },
        "firstName": {
          "type": "string",
          "description": "User's first name."
        },
        "lastName": {
          "type": "string",
          "description": "User's last name."
        },
        "phone": {
          "type": "string",
          "description": "The user's phone number."
        },
        "role": {
          "type": "string",
          "description": "The role of the user within the application.",
          "enum": ["admin", "employee", "customer"]
        },
        "state": {
          "type": "string",
          "description": "The US state or region associated with the user (e.g., 'TX', 'PR')."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user profile was created.",
          "format": "date-time"
        },
        "profilePhoto": {
          "type": "string",
          "description": "URL to the user's profile photo stored in Firebase Cloud Storage.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "role",
        "state",
        "createdAt"
      ]
    },
    "EmployeeProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "EmployeeProfile",
      "type": "object",
      "description": "Details specific to an employee, linked to a UserProfile.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the EmployeeProfile entity, same as the user's ID. (Primary Key)"
        },
        "userId": {
          "type": "string",
          "description": "Reference to the UserProfile entity's ID."
        },
        "employeeId": {
          "type": "string",
          "description": "A unique identifier assigned to the employee."
        },
        "address": { "type": "string" },
        "aptSuite": { "type": "string" },
        "city": { "type": "string" },
        "state": { "type": "string" },
        "zipCode": { "type": "string" },
        "businessName": { "type": "string" },
        "businessDescription": { "type": "string" },
        "hasDotVehicle": { "type": "boolean" },
        "affiliation": { "type": "string", "enum": ["Independent", "U-Haul Dealer", "U-Haul Employee"] },
        "source": { "type": "string" },
        "region": {
          "type": "string",
          "description": "The specific region or territory assigned to the employee."
        },
        "hireDate": {
          "type": "string",
          "description": "The date when the employee was hired.",
          "format": "date-time"
        },
        "status": {
          "type": "string",
          "description": "The current employment status of the employee.",
          "enum": ["applicant", "training", "active", "rejected"]
        },
        "onboardingStep": {
          "type": "number",
          "description": "The current step in the onboarding process."
        },
        "assignedJobIds": {
          "type": "array",
          "description": "An array of IDs for jobs currently assigned to this employee.",
          "items": {
            "type": "string"
          }
        },
        "availability": {
          "type": "string",
          "description": "An object where keys are date strings (YYYY-MM-DD) and values are booleans indicating employee availability."
        }
      },
      "required": [
        "id",
        "userId",
        "status"
      ]
    },
    "Job": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Job",
      "type": "object",
      "description": "Details of a specific moving job, including customer, assigned employees, and move logistics.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Job entity. (Primary Key)"
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the UserProfile ID of the customer who booked the job."
        },
        "employeeIds": {
          "type": "array",
          "description": "An array of UserProfile IDs for employees assigned to this job.",
          "items": {
            "type": "string"
          }
        },
        "state": {
          "type": "string",
          "description": "The state where the job primarily takes place."
        },
        "status": {
          "type": "string",
          "description": "The current status of the job.",
          "enum": ["new", "scheduled", "confirmed", "in-progress", "completed", "cancelled"]
        },
        "moveDate": {
          "type": "string",
          "description": "The scheduled date for the move.",
          "format": "date-time"
        },
        "pickupAddress": {
          "type": "string",
          "description": "The full address for the pickup location."
        },
        "dropoffAddress": {
          "type": "string",
          "description": "The full address for the dropoff location."
        },
        "moveSize": {
          "type": "string",
          "description": "The estimated size of the move."
        },
        "specialItems": {
          "type": "string",
          "description": "Description of any special items requiring specific handling."
        },
        "estimatedHours": {
          "type": "number",
          "description": "The estimated number of hours to complete the job."
        },
        "price": {
          "type": "number",
          "description": "The agreed-upon price for the moving service."
        },
        "notes": {
          "type": "string",
          "description": "Internal notes related to the job."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the job was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update to the job.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "state",
        "status",
        "moveDate",
        "pickupAddress",
        "dropoffAddress",
        "moveSize",
        "estimatedHours",
        "price",
        "createdAt",
        "updatedAt"
      ]
    },
    "Quote": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Quote",
      "type": "object",
      "description": "Represents a moving quote requested by a customer.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Quote entity. (Primary Key)"
        },
        "customerId": {
          "type": "string",
          "description": "Reference to the UserProfile ID of the customer requesting the quote."
        },
        "status": {
          "type": "string",
          "description": "The current status of the quote.",
          "enum": ["new", "processed", "expired"]
        },
        "details": {
          "type": "string",
          "description": "A JSON string containing detailed information about the quote."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the quote was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerId",
        "status",
        "details",
        "createdAt"
      ]
    },
    "JobApplication": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "JobApplication",
      "type": "object",
      "description": "Stores information for job applications submitted through the careers portal.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the JobApplication entity. (Primary Key)"
        },
        "name": {
          "type": "string",
          "description": "The full name of the applicant."
        },
        "email": {
          "type": "string",
          "description": "The applicant's email address.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The applicant's phone number."
        },
        "state": {
          "type": "string",
          "description": "The state where the applicant is seeking employment."
        },
        "experience": {
          "type": "string",
          "description": "Details about the applicant's work experience."
        },
        "resume": {
          "type": "string",
          "description": "URL to the applicant's resume document.",
          "format": "uri"
        },
        "status": {
          "type": "string",
          "description": "The current status of the job application."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the application was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "email",
        "phone",
        "state",
        "experience",
        "resume",
        "status",
        "createdAt"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "UserProfile",
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information.",
          "params": [
            {
              "name": "userId",
              "description": "The unique Firebase Authentication User ID (UID)."
            }
          ]
        }
      },
      {
        "path": "/employees/{employeeUid}",
        "definition": {
          "entityName": "EmployeeProfile",
          "schema": {
            "$ref": "#/backend/entities/EmployeeProfile"
          },
          "description": "Contains details specific to an employee.",
          "params": [
            {
              "name": "employeeUid",
              "description": "The unique Firebase Authentication User ID (UID)."
            }
          ]
        }
      },
      {
        "path": "/announcements/{announcementId}",
        "definition": {
          "entityName": "Announcement",
          "schema": {
            "type": "object",
            "properties": {
              "title": { "type": "string" },
              "content": { "type": "string" },
              "createdAt": { "type": "string", "format": "date-time" },
              "authorName": { "type": "string" }
            }
          },
          "description": "System-wide broadcasts for employees."
        }
      },
      {
        "path": "/quotes/{quoteId}",
        "definition": {
          "entityName": "Quote",
          "schema": { "$ref": "#/backend/entities/Quote" },
          "description": "Customer requested moving quotes."
        }
      },
      {
        "path": "/jobs/{jobId}",
        "definition": {
          "entityName": "Job",
          "schema": { "$ref": "#/backend/entities/Job" },
          "description": "Moving job logistics and status."
        }
      }
    ]
  }
}
